content:
  report:
    def_file:  simple_stats.yaml
    pattern:   _global
    dest_file: out3.txt
    format:    txt
    trigger:
      start:   top.cpu.core0.rob.stats.total_number_retired >= 162
      stop:    top.cpu.core0.rob.stats.total_number_retired >= 193
      tag:     t0
